import '$lib/config';
import '$lib/db'
import '$lib/twitch';

/* For testing what the configuration looks like; this makes sure to keep the
 * sensitive configuration keys sensitive so complete boobs don't leak stuff. */
// import { config } from '$lib/config';
// console.log(config.toString());


// =============================================================================


console.log('Application back end setup complete');